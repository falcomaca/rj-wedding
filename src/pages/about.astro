---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="es">
<head>
  <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
</head>

<body class="text-gray-800 bg-transparent">
  <Header />
</section>
  <main class="p-10 text-center">


    <section class="asistencia">

      <!-- TEXTO -->
       <div class="asistencia-texto">
  <span class="asistencia-titulo">
    Nos encantaría contar contigo
  </span>

        <p class="texto aviso">
          Sabemos que todavía es un poco pronto.
          La vida da muchas vueltas… y las agendas también.
          No os preocupéis, os lo recordaremos más adelante.
          <span>(pero desinvitarte no es una opción 😉)</span>
        </p>
      </div>

      <!-- SOBRE CON BOTÓN -->
      <div class="sobre-img-wrapper">
        <img
          src="/images/sobre.png"
          alt="Sobre abierto"
        />
  <!-- SELLO -->
  <img
    src="/images/sello-dorado.png"
    alt="Sello"
    class="sello-img"
  />

        <a
          href="https://docs.google.com/forms/d/1XpecWA5AQnOMow34dJx7eN8VrkqoTrzmFZZTgMY5QWw/edit"
          target="_blank"
          class="btn-rsvp sobre-btn"
        >
          CONFIRMAR ASISTENCIA
        </a>

        <p class="nota-form">
          Se abrirá el formulario de confirmación
        </p>
      </div>

    </section>

  </main>
  <!-- FLORAL BOTTOM -->

  <Footer />

<style>
    	  @font-face {
  font-family: "Boda";
  src: url("/fonts/abadi-mt-extra-light.ttf") format("truetype");
}
    /* ===================== HERO ===================== */
body {
  font-family: "Boda", sans-serif;
}
  /* ===================== FLORAL TOP ===================== */
.floral-top {
  width: 100%;
  overflow: hidden;
  margin-top: -1px; /* pega al header */
}

.floral-top img {
  width: 100%;
  max-height: 280px;     /* desktop */
  object-fit: cover;
  object-position: center;
  display: block;
  opacity: 0.95;        /* elegante */
}
.floral-bottom {
  width: 100%;
  overflow: hidden;
  margin-top: 3rem;   /* separa del sobre */
}

.floral-bottom img {
  width: 100%;
  max-height: 220px;
  object-fit: cover;
  object-position: center;
  display: block;
  opacity: 0.95;
}
/* ===================== ASISTENCIA ===================== */
.asistencia-titulo {
  display: block;
  margin-bottom: 1.2rem;

  font-size: 1.20rem;
  letter-spacing: 0.35em;
  text-transform: uppercase;

  color: #8b9a77;
  text-align: center;
}
.asistencia {
  max-width: 900px;
  margin: 0 auto;
}

/* ===== TEXTO ===== */
.asistencia-texto {
  margin-bottom: 3rem;
  padding: 0 1.5rem;
  text-align: center;   /* 👈 CLAVE */
}

.texto.aviso {
  font-size: 1.15rem;
  line-height: 1.7;
  max-width: 920px;
  margin: 0 auto;       /* 👈 CLAVE */
}

.texto.aviso span {
  display: block;
  margin-top: 0.8rem;
  color: #666;
}

/* ===== SOBRE IMAGEN ===== */
.sobre-img-wrapper {
  position: relative;
  width: 520px;      /* ⬅️ MÁS GRANDE */
  max-width: 90vw;   /* responsive */
  overflow: visible; /* 🔑 CLAVE ABSOLUTA */
  margin: 5.5rem auto 4rem;
}

.sobre-img-wrapper > img:not(.sello-img) {
  width: 100%;
  display: block;
}
.sello-img {
  position: absolute;
  width: 90px;        /* 👈 PRUEBA 70–100 */
  height: auto;
  top: 55%;
  left: 45%;
  transform: translateX(-50%);
  z-index: 3;
  pointer-events: none;
}

/* ===== BOTÓN ===== */
.btn-rsvp {
  display: inline-block;
  padding: 0.95rem 2.6rem;
  background: linear-gradient(to bottom, #7c8c66, #6f7f5f);
  color: #ffffff;
  text-decoration: none;
  font-size: 1.05rem;
  letter-spacing: 0.08em;
  border-radius: 999px;
  box-shadow:
    0 10px 22px rgba(0,0,0,0.22),
    inset 0 1px 2px rgba(255,255,255,0.25);
  transition: all 0.25s ease;
}


/* Posición del botón SOBRE la imagen */
.sobre-btn {
  position: absolute;
  top: 35%;             /* ajusta aquí */
  left: 30%;
  transform: translate(-24%, -50%);
}

/* Nota */
.nota-form {
  position: absolute;
  top: 39%;          /* ajusta aquí */
  left: 47%;
  transform: translateX(-50%);
  font-size: 0.85rem;
  color: #888;
}

/* ===================== RESPONSIVE ===================== */
/* ===================== MÓVIL ASISTENCIA ===================== */
@media (max-width: 768px) {
    .floral-bottom img {
    max-height: 160px;
  }
  .floral-top img {
    max-height: 180px;
    object-position: center;
  }
}
@media (max-width: 768px) {

  /* Texto superior */
  .asistencia-texto {
    margin-bottom: 2rem;
  }

  .texto.aviso {
    font-size: 1rem;
      padding: 0 1.25rem;  
    line-height: 1.6;
    max-width: 100%;
  }

  /* Sobre */
  .sobre-img-wrapper {
    width: 85vw;        /* antes 90vw → un poco más estrecho */
    max-width: 360px;   /* controla tamaño máximo en móvil */
    margin: 2rem auto 3rem;
  }


  /* Botón */
  .sobre-btn {
    top: 30%;           /* bájalo un poco */
    left: 46%;
    font-size: 1.75rem;
    transform: translate(-50%, -50%);
    white-space: nowrap; 
    font-size: 0.9rem;
    padding: 0.75rem 0.75rem;
  }

  /* Nota formulario */
  .nota-form {
    top: 34%;
    font-size: 0.75rem;
    text-align: center;
    width: 90%;
  }

  /* Sello */
  .sello-img {
    width: 70px;        /* 👈 tamaño correcto en móvil */
    bottom: 8%;
  }
}



</style>

</body>
</html>